# AI Rules (Model-Agnostic)

This folder contains project rules intended for any AI model or agent, not tied to Cursor-specific behavior.

Subfolders:

- `main/`: Core numbered rules used for all requests.
- `actions/`: Action-specific checklists (`add`, `remove`, `refactor`).

Keep these rules concise, test-oriented, and repository-specific.
